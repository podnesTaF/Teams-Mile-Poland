import { LoadingScreen } from "@/components/ui/loading-screen";

/** Route-level fallback shown while a locale page's server work streams in. */
export default function Loading() {
  return <LoadingScreen />;
}
