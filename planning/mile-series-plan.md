# ABM Mile Series: Multi-Event + User Profiles + Check-in

## Context

The site currently serves one completed team event (`warsaw-2026`) with a config-driven event registry (`src/lib/events/registry.ts`) and a frozen legacy registration stack (teams/runners/slot_counter tables, magic links, Stripe 50 PLN flow). The organizer is now running a series of **5 individual mile events** at the same stadium (Stadion Podskarbińska, Warsaw):

| Slug | Date | Time | Status at launch |
|---|---|---|---|
| `mile-2026-08-01` | 2026-08-01 | 9:15–12:15 | registration_open |
| `mile-2026-08-08` | 2026-08-08 | 17:30–20:30 | registration_open |
| `mile-2026-08-15` | 2026-08-15 | 9:15–12:15 | upcoming |
| `mile-2026-08-22` | 2026-08-22 | 17:30–20:30 | upcoming |
| `mile-2026-08-29` | 2026-08-29 | 9:15–12:15 | upcoming |

New requirements (user-confirmed decisions):
- **Capacity per event: 30 free + 20 paid slots at 5 PLN via Stripe** (Stripe PLN minimum is 2 PLN — fine).
- **User accounts**: email+password **and** Google OAuth; email verification required. Profile fields (separate): first name, surname, date of birth, sex, running club (**free-text field — no team entity** for these events; heat grouping is manual).
- **Personal profile page** with user data + their registrations/tickets.
- **Check-in by staff via admin panel**: find runner (search/QR), assign bib, mark checked-in; mark no-shows.
- **Per-event timetables** shown on the site.
- Legacy warsaw-2026 pages (results, /team, /join, /ticket) keep working untouched.

## Architecture decisions

1. **Auth: Better Auth** with Drizzle adapter. Minimal Next surface (one catch-all route handler + `nextCookies()` plugin using async `cookies()` — matches this custom Next 16.2.6 build), no dependency on middleware (this build uses `proxy.ts`). Email+password, Google, email verification (hooked to existing Resend), profile fields via `user.additionalFields`. Auth tables hand-written in `src/db/schema/` so **drizzle-kit stays the only migration tool**. Existing HMAC sessions stay for admin + legacy team flows. Route protection enforced in **server pages/actions** via `auth.api.getSession({ headers: await headers() })`, not in proxy.ts.
2. **Events stay in the config registry** (extended), not a DB table. Only mutable operational state (registrations, counters, pending checkouts) goes to DB, keyed by `event_slug text` (no FK). Caps/prices come from config into atomic `UPDATE … WHERE claimed < cap` — the same pattern as today's `createFreeRegistration`.
3. **Legacy tables frozen** — no backfill of users from old runners, no changes to existing tables. All migrations purely additive.
4. **Participation lifecycle**: `registered → checked_in | no_show` (+ `cancelled` for admin corrections). Bib = integer unique per event (partial unique index), assigned at check-in; UI suggests `max(bib)+1`, allows manual entry, retries on unique violation.
5. **Paid slots are claimed at checkout creation and released on expiry** (new vs legacy: legacy paid was uncapped). `checkout.session.expired` webhook + cron sweep release abandoned claims.

## Next.js 16.2.6 constraints (custom build — see `node_modules/next/dist/docs/`)

- `params`/`searchParams`/`cookies()`/`headers()` are **async-only**; use `PageProps<'/route'>` helpers.
- `proxy.ts` (already exists, next-intl only) — do not add middleware.ts.
- `revalidateTag(tag, 'max')` needs 2nd arg; `updateTag()` in server actions; never wrap session/counter reads in `'use cache'`.
- Parallel route slots need explicit `default.tsx` (existing `@modal` slot — check when touching `[locale]` routes).

## DB schema (new files in `src/db/schema/`, export from `index.ts`)

```ts
// auth.ts — per Better Auth Drizzle adapter; VERIFY shape against
// `npx @better-auth/cli generate` output before writing migration.
users: id text PK, name, email unique, emailVerified bool, image,
  // additionalFields:
  firstName, lastName, dateOfBirth date, sex pgEnum('user_sex',['M','F']),
  club text, phone, locale text default 'pl', createdAt, updatedAt
sessions / accounts / verifications — standard Better Auth tables

// event-registrations.ts
participationStatusEnum = pgEnum("participation_status",
  ["registered","checked_in","no_show","cancelled"])
eventRegistrations: id uuid PK, eventSlug text, userId → users.id,
  status default 'registered', bib integer, paymentStatus (reuse existing enum),
  freeSlot bool, stripeSessionId, terms bool, locale, checkedInAt, createdAt
  + uniqueIndex(eventSlug, userId)
  + uniqueIndex(eventSlug, bib) WHERE bib IS NOT NULL

// event-counters.ts — per-event version of slot_counter
eventSlotCounters: eventSlug text PK, freeClaimed int, paidClaimed int, updatedAt
  // claim: UPDATE … SET free_claimed = free_claimed+1
  //        WHERE event_slug=$s AND free_claimed < $capFromConfig RETURNING …
  // rows seeded lazily: INSERT … ON CONFLICT DO NOTHING before claim

// pending-event-registrations.ts
pendingEventRegistrations: id uuid PK, eventSlug, userId,
  stripeSessionId unique, locale, expiresAt (15 min), createdAt
  // deletion always pairs with paidClaimed decrement in same transaction
```

Migrations: `0005` (auth tables), `0006` (event registration tables) via `npm run db:generate`. Apply to a **Neon branch first**, then prod.

## Registry/type extensions

`src/lib/events/types.ts` — extend `EventSummary`:
```ts
eventType: "team" | "individual";
timeRange?: { start: string; end: string };   // "09:15" local
capacity?: { free: number; paid: number };    // 30 / 20
pricePln?: number;                            // 5
timetable?: TimetableBlock[];                 // heats in the 3h block, i18n label keys
```
`src/lib/events/registry.ts` — add the 5 events; new selectors `getOpenEvents()`, `getUpcomingEvents()`, `getEventBySlug()` / `getEventOrThrow()`. Keep `getFeaturedEvent()` for hero.

## Implementation phases (each leaves the site deployable)

### Phase 1 — Event series model + public pages (no auth)
- Extend `types.ts` + `registry.ts` (events start as `upcoming`); timetable config in `src/lib/events/timetables/` (shared template fn — same stadium, two time patterns).
- `src/app/[locale]/events/[slug]/page.tsx` — detail: date/time, venue (reuse `EVENT.venue` from `src/lib/marketing/event.ts`), timetable, capacity meter, register CTA (disabled "opens soon" until Phase 4). `generateStaticParams` from registry.
- `src/components/landing/event-series.tsx` cards section; wire into `landing-view.tsx`; point hero/`register-cta.tsx` at featured event's `/events/[slug]`.
- i18n: `events` namespace in `src/messages/{pl,en,ua}.json`.

### Phase 2 — Better Auth + profile
- `npm i better-auth`; `src/db/schema/auth.ts`; migration 0005.
- `src/lib/auth/better-auth.ts` — betterAuth({ drizzleAdapter, emailAndPassword + requireEmailVerification, emailVerification → Resend (`src/emails/verify-email.tsx`, reuse `src/emails/components.tsx` patterns), socialProviders.google, user.additionalFields, plugins: [nextCookies()] }).
- `src/lib/auth/auth-client.ts` (createAuthClient); `src/app/api/auth/[...all]/route.ts` (toNextJsHandler). Note: proxy matcher already excludes `/api`.
- **Spike-verify auth works on this custom Next 16 before building UI on it** (sign-up/sign-in round trip). Fallback: Better Auth core + thin cookie bridge over existing `src/lib/auth/session.ts` HMAC helpers.
- Auth pages under `src/app/[locale]/auth/`: sign-in, sign-up, verify-email, forgot/reset-password (react-hook-form + zod, existing form styles).
- `src/lib/auth/user-session.ts` — `getUser()` / `requireUser()` / `isProfileComplete()`.
- `src/app/[locale]/profile/page.tsx` + `src/features/profile/{actions,schemas,components}` — profile form (firstName, lastName, DOB, sex, club, phone). Header gets sign-in/profile link.

### Phase 3 — Event registration + payment + tickets
- Schema files + migration 0006.
- `src/features/event-registration/data.ts` — `claimFreeSlot`/`claimPaidSlot`/`releasePaidSlot`, `createRegistration`, `promotePendingEventRegistration` (idempotent, mirror `src/features/registration/data.ts` promotePendingRegistration), `getEventCounters`, `getUserRegistrations`.
- `src/features/event-registration/actions.ts` — `registerForEvent(slug)`: guards (session + emailVerified + profile complete + `registration_open` + no dup — unique index backstop); free path → insert + ticket email; paid path → claim paid counter → Stripe checkout (5 PLN, metadata `{ kind: "event_registration" }`) → pending row; release counter on Stripe error.
- `src/app/api/stripe/webhook/route.ts` — branch on `metadata.kind`; handle `checkout.session.expired` → delete pending + release paid counter. **Legacy path literally unchanged.**
- Tickets: add `signEventTicket(registrationId)` in `src/features/ticket/sign.ts` (distinct purpose string); `src/app/[locale]/tickets/[registrationId]/page.tsx`; QR via existing `src/features/ticket/qr.ts`; `src/emails/event-ticket.tsx` confirmation.
- `src/app/[locale]/events/[slug]/register/page.tsx` — auth-gated confirm page (summary + terms + submit; success/cancel states). Live capacity on event detail.
- Profile "My registrations" list (event, status, payment, ticket link).
- `src/app/api/cron/cleanup/route.ts` — sweep expired pending rows + release paid slots (CRON_SECRET-guarded).

### Phase 4 — Go live
- Flip `mile-2026-08-01` + `mile-2026-08-08` to `registration_open`; landing features soonest open event.

### Phase 5 — Admin check-in & roster
- `src/features/admin/events-data.ts` — roster query (event_registrations ⋈ users), search name/email/bib, `suggestNextBib(slug)`.
- `src/features/admin/checkin-actions.ts` — admin-session-guarded: `assignBibAndCheckIn(regId, bib)` (transaction, retry on unique violation), `markNoShow`, `revertToRegistered`.
- `src/app/[locale]/admin/events/[slug]/page.tsx` — roster + status filters + XLSX export (extend `src/features/admin/export-runners.ts` pattern; include DOB/sex/club + age category computed from `AGE_CATEGORIES` config).
- `src/app/[locale]/admin/events/[slug]/checkin/page.tsx` — search + QR scan (decode ticket URL → registrationId+sig, verify server-side) + bib input pre-filled with suggestion; check-in / no-show buttons. Legacy `/api/ticket/check-in` stays.
- Admin home links to per-event pages.

### Phase 6 — Mailings generalization (post-launch follow-up)
Current `src/features/mailings/schedule.ts` is hardcoded to 2026-06-27. Parameterize by event date/timeRange from the registry; drop captain-nudge for individual events; key `email_log` idempotency by (eventRegistrationId, kind). **Until then: keep the lifecycle cron scoped to legacy runners only** so it can't email new-event participants.

## New env vars
`BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` (or derive from `NEXT_PUBLIC_APP_URL`), `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`. Reuse existing: `DATABASE_URL`, `RESEND_API_KEY`, `STRIPE_*`, `SESSION_SECRET`, `ADMIN_PASSWORD`, `CHECKIN_API_KEY`, `CRON_SECRET`. Stripe dashboard: subscribe webhook to `checkout.session.expired`; Google console: redirect URI `…/api/auth/callback/google`.

## Verification (per phase)
1. `npm run typecheck && npm run lint && npm run build` — build is the guard for custom-Next pitfalls (async params, `@modal` `default.tsx`, no `'use cache'` around session reads).
2. Migrations on Neon branch; confirm legacy tables untouched.
3. Auth: sign-up → Resend verification arrives → unverified blocked from registering → Google sign-in → profile-completeness gate.
4. Registration: concurrent-burst claim of 30 free slots holds cap; 31st → Stripe; `stripe listen --forward-to localhost:3000/api/stripe/webhook` → completed promotes + ticket email with scannable QR; abandoned checkout releases paid counter; duplicate registration rejected.
5. Check-in: search/QR → bib suggested → assigned; duplicate bib rejected; no-show flips status; XLSX has new fields.
6. All 3 locales render new pages; legacy `/join/[code]`, `/team/[code]`, `/ticket/[runnerId]`, results section still work.

## Risks
- **Better Auth × custom Next 16.2.6** — biggest unknown; de-risked by the Phase 2 spike before dependent UI. Fallback documented above.
- Paid-slot leakage on abandoned checkouts — expired webhook + cron sweep; monitor paidClaimed vs paid rows.
- Webhook regression for legacy flow — branch only on new metadata, don't touch legacy path.
- Bib races — partial unique index is source of truth; UI retries.

## Progress log
- 2026-07-12 — PRD #7 slice 1 (issue #9): additive migration 0010 (`legacy_participations`, `user_broadcasts`, `user_broadcast_log`, `users.marketing_opt_out`) + one-time first-event import script (`npm run import:first-event`, dry-run default / `--write`). Dry-run against live data: 83 runners → 78 unique emails (66 new users, 12 existing attach-only), 13/23 results entries matched, 10 unmatched (on-site heat 3 + Cyrillic-registered names), 0 ambiguous. Migration apply + `--write` + idempotency re-run still need a Neon branch (no neonctl here).
- 2026-07-13 — PRD #7 prefactor slice (issue #8): admin monolith split into `/admin` (stat cards + series table w/ per-event registration counts), `/admin/inquiries`, `/admin/legacy` (frozen warsaw-2026 teams/runners + exports), `/admin/users` stub; shared `AdminShell` nav on all admin pages (incl. mailings, roster, check-in) with router-aware `Link`s — the old `<a>` static-export lint errors are gone. Feature module split: `action-helpers` / `inquiries-*` / `legacy-*` / `overview-data` + shared `format`/`Stat`/`StatusPill`/`DownloadLink`/`NoDatabaseNotice`. Fixed latent FK crash: `deleteTeamCascade` now detaches `broadcasts.team_id` before deleting a team. Verified live: login → overview → inquiries → legacy → users → roster/check-in over HTTP; unauthenticated requests stream a redirect with no data leak.
- 2026-07-13 — Issue #9 executed on the **live DB** (owner's call): `db:migrate` applied 0009 (pending `confirmation` enum value that had never reached live) + 0010; `--write` created 66 users / 78 participations (13 attended), re-run was a full no-op, and a field-by-field snapshot diff confirmed zero profile changes on the 19 pre-existing users. Issue #9 closed. Claim-flow caveat for the broadcast slice: imported passwordless users dead-end on plain `/auth/sign-up` and Google sign-in (`account_not_linked`); broadcast copy must link to the events register flow (guest-registration resend path), per the PRD.
- 2026-07-13 — PRD #7 slice (issue #10): user management on top of #8/#9 — `/admin/users` (search + four combinable filters: verified, first-event attendance, Aug-registered), `/admin/users/[id]` (profile + legacy⋃series history unioned chronologically), and `deleteUser` (FK cascade, confirm dialog) + `resendUserVerification` (Better Auth send-verification → `/events`, unverified-only). New `users-data.ts` / `users-actions.ts`; reuses the admin shell + table/pill/confirm/no-db idioms. Data layer + delete cascade verified against a Neon branch (filter partitions sum, combined filter + search narrow correctly, unioned history renders, throwaway-user delete cascaded participation + registration). Resend email not fired live (avoids messaging a real imported person); path is identical to the proven `registerAsGuest` resend. Branch `feat/admin-users-management`, commit `cef46a3`; issue #10 closed. Next: participant management (#12) and broadcasts/unsubscribe (#11) are already in-flight in the working tree.
- 2026-07-13 — PRD #7 slice (issue #11): user broadcasts + audience segments + unsubscribe, commit `214ba4b`. New `event-mailings` module parallel to the frozen legacy mailer: `user-segments.ts` resolves `all` / `first_event_attended` / `first_event_no_show` / `registered:<slug>` / `registered_any_aug` / `not_registered_aug` over users × legacy participations × registrations, with `marketing_opt_out` excluded in one `whereForSegment` choke point; "Aug events" = every individual registry event (`getIndividualEvents()`). `user-broadcast.ts` — `sendUserBroadcast` / `resendUserBroadcast` persist + Resend-send + dedup per `(user, broadcast)` (already-logged set checked before send, so re-send double-emails nobody; only successes logged so failures retry and never abort). `/admin/mailings` gains the user-segment composer (picker labels carry live recipient counts) + past-user-broadcasts table with Re-send; legacy sections untouched. `BroadcastEmail` gains an optional trilingual unsubscribe footer localized by stored locale (legacy send omits it → byte-identical). Public `/unsubscribe?token=<signed>`: HMAC user token via the existing signing helper under a distinct `unsubscribe` purpose, idempotent + non-revealing, sets the flag; trilingual success/error pages + new `unsubscribe` i18n namespace (pl/en/ua). Verified: typecheck + build (`/unsubscribe` Dynamic) + lint clean; unsubscribe token crypto round-trip (valid→ok; tampered/dot-less/empty/purpose-replay→invalid). **Still needs a Neon branch + Resend key** to verify segment resolution against seeded data, live send→dedup→re-send, the confirmation-page flag-set, and the claim-flow spot check — issue #11 left open. Note: the uncommitted `admin/users*` files in this tree are #10 leftovers, excluded from the commit.
- 2026-07-13 — PRD #14 prefactor slice (issue #15): completed individual events keep their event detail page, commit `f38a045`. `generateStaticParams` on `/[locale]/events/[slug]` switched from `getSeriesEvents()` (drops completed) to the pre-existing `getIndividualEvents()` (individual events at any lifecycle status), so a race night that flips to `completed` no longer falls out of the static export and 404s — the #14 gallery teaser/back-link and media-live mailing CTA all point at this page. No new selector needed (the broadcast segments already had `getIndividualEvents`); only its docstring widened to record the new consumer. `getSeriesEvents` keeps non-completed semantics for landing cards/mailings. No media, no new UI, no new strings — the completed lifecycle state (status badge, `banner--ok` "Race complete", results CTA) and its `events.detail.states.completed.*` copy already exist in pl/en/ua. Verified: typecheck + build (`.next` cleared) + lint clean, no new errors; `/verify` flipped `mile-2026-08-01` → `completed`, rebuilt, confirmed all of `/{pl,en,ua}/events/mile-2026-08-01.html` prerender with `status--completed` + localized banner + localized CTA, then reverted the flip. Issue #15 closed. Note: the uncommitted `admin/events*` / `admin/users*` / `roster-actions`/`users-actions` files in this tree are #12 (participant management) work-in-progress, excluded from this commit.
- 2026-07-13 — PRD #14 slice (issue #16): Drive-backed post-event gallery page, commit `8b974f2`. `EventSummary.media?: { driveFolderId }` + `getGalleryEvents()` (completed individual events with media) drive the new SSG ×3 route `/[locale]/events/[slug]/gallery`. Build-time `listEventMedia` (`src/lib/events/media.ts`) lists the "anyone-with-link" folder via Drive API v3 `files.list` (`GOOGLE_DRIVE_API_KEY`, paginated, `image/*`+`video/*`, `name_natural`+local numeric sort), fail-loud on any listing error or missing key; left uncached so each build re-lists yet stays statically prerendered. Pure `drive-urls.ts` helpers (thumbnail/large/download/preview/album) split out so the lightbox client bundle never pulls in the listing code. Page = header (name/date/back-link/open-album) + `generateMetadata` (OG title + first photo as share image, story #21); `gallery-lightbox.tsx` client island = photo grid + separate videos section w/ play badges, `{ openIndex }`, open/close + arrow-key/swipe/on-screen nav (range-clamped), large image, per-photo download, inline Drive video preview, no fetching. New `events.media.*` (pl/en/ua, key parity checked); fully public, no auth. `gallery.css` scoped under `.ace-landing`. Verified: typecheck + build + lint clean (new files lint-clean; `<img>` disabled per "Drive is the CDN"). **Build-verified fail-loud + static params**: temp-marking a mile event `completed`+media generated the route ×3 locales and aborted the build both with no key ("GOOGLE_DRIVE_API_KEY is not set") and with a fake key (real Drive `400` "Drive listing failed for folder …"); reverted → clean build shows the route with 0 paths (no completed individual event has media yet). **Live `/verify` blocked** — the grid→lightbox→download→video→album round-trip needs a real API key + shared folder (photos+video) on a completed event, unavailable here (Neon-branch-style caveat); the two runtime-behavior criteria + the `/verify` line stay unticked, issue #16 left open. The media-live mailing, admin send button, and event-page teaser strip are separate #14 slices, not in this commit. Note: uncommitted `admin/*` + `news-*` files in this tree are #12/#20 WIP, excluded.
- 2026-07-13 — PRD #19 slice (issue #20): on-site news slice 1 — `news_articles` table + admin draft CRUD, commit `bda1485`. Additive migration `0011` creates `news_articles` per the frozen PRD contract (uuid pk, unique `slug`, per-locale `title_*`/`body_*` (not jsonb), nullable `cover_image_url` + `published_at`, timestamps); emitted SQL read, legacy tables untouched. New `news-data.ts` (`listArticles` newest-first, `getArticleById`) + `news-actions.ts` (`createArticle` / `updateArticle` / `deleteArticle`) mirror the admin users data/actions split: `requireAdmin`, FormData, `?msg=` redirects, `revalidatePath`. Admin routes `/admin/news` (list + draft/published pill + delete-behind-confirm), `/admin/news/new`, `/admin/news/[id]`; shared plain-`useState` `NewsArticleForm` with all six trilingual fields and EN-title→kebab slug auto-suggest (`news-slug.ts`, transliteration-light, ł→l); News nav item + admin-index card. `published_at` stays null / `cover_image_url` empty — publish, public pages, images, and the `news` i18n namespace are later slices (#21-#23); admin UI English-only. Slug collisions rejected via the unique index with a clear `?msg=` error. Verified: typecheck + build (three `/admin/news*` routes generate) + lint clean, no new errors. **Still needs a Neon branch** for the `/verify` DB round-trip (migrate → create draft in pl/en/ua → edit → delete): no neonctl/psql here, so the migration was not applied to the shared dev DB and the create/edit/delete acceptance boxes + #9-style live check stay unticked — issue #20 left open. Note: uncommitted `events/.../gallery` + `lib/events/media`/`drive-urls` files in this tree are #16/#17 media-gallery WIP, excluded from this commit.
- 2026-07-13 — PRD #19 slice (issue #21): on-site news slice 2 — publish/unpublish + public `/news` pages, commit `3df179b`. `publishArticle` validates all six trilingual text fields against the *stored* row (save-then-publish flow), stamps `published_at`, idempotent (re-publish keeps the original date); cover-image requirement deferred to the images slice per contract. `unpublishArticle` clears `published_at`. New shared `revalidateNews()` fires on every mutation (create/update/publish/unpublish/delete) and invalidates the public list, article, and landing routes across all locales via the route-pattern form of `revalidatePath` (`/[locale]/news`, `/[locale]/news/[slug]`, `/[locale]`) — repo has no Cache Components, so `revalidatePath` is the primitive (verified against in-repo Next 16 docs). **Draft-completeness rule relaxed then re-tightened:** drafts may be saved partially (prepare across sessions), but completeness is enforced at publish *and* on edits to an already-published article (a live article can't be blanked out — caught in self-review). Public routes: `/[locale]/news` (SSG ×3, published-only, newest-first by `published_at`, cards with title/localized date/markdown-stripped auto-excerpt, translated empty state) + `/[locale]/news/[slug]` (dynamic-on-demand, `react-markdown` body with no raw HTML + off-site links, draft/unknown slug → `notFound()`); by-slug query memoized with React `cache` (shared with `generateMetadata`); `listPublishedArticles` tolerates a not-yet-migrated store *at build phase only* (`NEXT_PHASE` guard) so SSG doesn't fail pre-migration while runtime still surfaces real errors. New `src/features/news/` (`text.ts` excerpt+locale pickers, `date.ts` Warsaw-pinned formatter, `article-body.tsx`). Admin edit page gains Publish/Unpublish/View-public-page controls. New `news` i18n namespace in pl/en/ua (key parity checked); new dep `react-markdown`. Verified: typecheck + build (list SSG ×3, slug dynamic) + lint clean, helpers exercised directly. **Still needs a Neon branch** for the `/verify` DB round-trip (publish → visible in all three locales → edit-visible-immediately → unpublish → 404; negative missing-ua-body rejection) — runtime publish/unpublish/cache-invalidation criteria stay unticked, issue #21 left open. Only my news files committed; message catalogs staged as news-only hunks (the co-resident `events.gallery.*` teaser keys are #17 WIP, excluded).
- 2026-07-13 — PRD #7 slice (issue #12): participant management — admin-register + remove-registration, commit `a1c93e8`, issue closed. `adminRegisterUserForEvent` (`users-actions.ts`) on `/admin/users/[id]`: event picker over `getSeriesEvents()` (non-completed individual), guards per the frozen contract (email verified → event exists & individual → status ≠ `completed` → no existing registration), then the shared `createFreeRegistration` + `sendEventTicketEmail` path; the register card is replaced by a note for unverified accounts and the action re-checks the verified-email guard server-side, so the verified-email invariant holds on every path. `removeRegistration` (new `roster-actions.ts`) on `/admin/events/[slug]`: per-row `ConfirmSubmit` → delete (its `event_email_log` rows cascade), status filter preserved on the `?msg=` redirect. No new i18n keys (admin English-only), no new subsystems — reuses the redirect+query-flag pattern and existing idioms. Verified live over the **no-JS multipart form-POST surface** against the shared Neon DB (admin cookie minted from `SESSION_SECRET`, dev server run with `RESEND_API_KEY=""` so the ticket send no-ops per owner's call — email to be triggered manually via admin): happy path created the row with `event_email_log` confirmation count 0; already-registered / unverified (UI note + no form + server guard) / warsaw-2026 all blocked with clear `?msg=` feedback; remove deleted the row; final DB sweep left zero test rows. typecheck + build + lint clean, no new errors. **Nuance:** the only `completed` registry event (`warsaw-2026`) is `eventType: "team"`, so it trips the earlier `eventType !== "individual"` guard ("Event not found.") — the `status === "completed"` branch is correct but only independently reachable once a completed *individual* event exists (per #15). Note: uncommitted `lib/events/{registry,types,media,drive-urls}` + `events/.../gallery` + `messages/*` + `landing/invite` in this tree are #14-#18 media-gallery/news WIP, excluded from this commit.
- 2026-07-13 — PRD #14 slice (issue #17): event-page media section — teaser strip + coming-soon state, commit `d5ff0cf`, issue closed. The completed event detail page (`/[locale]/events/[slug]`) now renders exactly one media state: **published** (`event.media` set) → new `EventMediaTeaser` server component = strip of the first 8 photos (`TEASER_COUNT`) + pluralized video count, every cell + CTA linking to `/events/<slug>/gallery`; **completed w/o media** → `media-soon` "coming soon" note; **non-completed** → nothing. The teaser reuses #16's build-time `listEventMedia` (fail-loud, no request-time calls) + `driveThumbUrl`, and `gallery.css` (now imported into the event page). New `events.media.*` in pl/en/ua — `teaserHeading`, `viewAll`, `videoCount` (ICU plural), `comingSoon`. Verified: typecheck + build + lint clean (event route stays SSG ×3, teaser file lint-clean w/ the same `<img>`-disable "Drive is the CDN" note). **Coming-soon + non-completed verified against real prerendered HTML** (temp-`completed` mile event, no media → `media-soon` note in en/pl/ua + no teaser; `registration_open`/`upcoming` → zero media markup; reverted). **Teaser render + click-through verified via a build-time `listEventMedia` stub** (12 photos + 3 videos → exactly 8 cells, `3 videos`/`3 filmy`/`3 відео`, all links → the gallery route generated ×3; reverted stub + config). ICU plural grammar checked via `intl-messageformat` for 1/2/3/5/12/22 (pl few/many, ua invariant, en one/other). **Residual (external, #16-style):** real Drive thumbnails loading in a live browser needs a real `GOOGLE_DRIVE_API_KEY` + shared folder — rests on #16's proven `driveThumbUrl`; the teaser's own logic is fully verified. Only my six slice files committed (page, teaser, `gallery.css`, three catalogs); other uncommitted tree files are #16/#18/#20-#23 WIP.
- 2026-07-13 — PRD #14 slice (issue #18): media-live mailing — enum migration, template, admin send button, commit `22af554`. Additive migration `0012` adds one `media_live` value to the `event_email_kind` enum (`ALTER TYPE … ADD VALUE`, emitted SQL read, legacy tables untouched). New manual (non-scheduled) mailing kind lives outside the cron chain: `sendMediaLiveMailing(slug)` (`event-mailings/media-live.ts`) guards completed-individual-with-media (throws `MediaLiveNotEligibleError` before any DB/email work), reuses `eligibleForEvent`, logs idempotently per `(event_registration_id, 'media_live')` via the existing unique-index + `onConflictDoNothing` pattern, returns `{ eligible, sent, skipped, failed }`; admin-gated wrapper `media-live-actions.ts` (`requireAdmin` → send → `?msg=` counts redirect, `redirect` outside the try so it isn't swallowed). `EventMediaLiveEmail` (`src/emails/event-media-live.tsx`) reuses the `EmailShell`/`HeroBand`/`Btn` shell; copy `mediaLiveMailContent` in `event-mailings/copy.ts` (pl/en/ua) with a locale-correct `/events/<slug>/gallery` CTA (pl unprefixed, en/ua prefixed — matches `localePrefix: as-needed`, same logic as `makeEventTicketUrl`). "Send photos-live email" button on `/admin/events/[slug]` (English-only, `ConfirmSubmit`) rendered only when `status === "completed" && media`. Verified: typecheck + build (gallery route SSG ×3, 0 paths — no event has media yet) + lint clean (touched files eslint exit 0; the 56 baseline problems are pre-existing `set-state-in-effect` in untouched files). **Email surface driven live**: rendered `EventMediaLiveEmail` through `@react-email/render` (Resend's renderer) for pl/en/ua — correct localized subject/title/greeting/intro/outro, gallery button ("Otwórz galerię"/"Open the gallery"/"Відкрити галерею"), working CTA href each. **Still needs a Neon branch** (criteria #4 + #7 unticked): first-send-counts / second-send-all-skipped idempotency round-trip + one `media_live` log row per registration — not driven here because this env has a live shared `DATABASE_URL` + `RESEND_API_KEY` (would mail real registrations). Admin-button pixel not driven (needs admin session + live DB + a configured completed-media event); gate is a 6-line conditional the build validated. GitHub issue-comment + checkbox-tick blocked by the write-permission classifier — issue left open pending owner action. Only my nine slice files committed; other uncommitted tree files (`landing/invite`, settings) excluded.
- 2026-07-13 — PRD #19 slice (issue #22): on-site news slice 3 — landing latest-news section, commit `59edb10`, issue closed. New `LatestNews` server component (`components/landing/latest-news.tsx`) wired into `LandingView` after `EventSeries`: renders the 3 newest published articles as cards (localized date, title→`/news/[slug]`, markdown-stripped excerpt, read-more) + an "all news" CTA to `/news`, and **returns `null` entirely when nothing is published** — no heading, no empty box (PRD story #8). Data via new `listLatestPublishedArticles(limit=3)` (`news-data.ts`), sharing slice-2's build-phase empty-list guard so the landing prerenders without a DB. Cache invalidation needed **no change** — slice-2's `revalidateNews()` already covers `/[locale]` so publish/unpublish/edit refresh the landing without a deploy. Shared `.news-card*` styles **moved** from `news.css` into `landing.css` (single source of truth; `/news` imports `landing.css` first, so its cards are unaffected) + new `.news-sec*` section chrome (3-col grid, head w/ eyebrow+title+all-news button). New `news.landingTitle` key in pl/en/ua; `allNews`/`readMore`/`eyebrow` reused. Verified: typecheck + build (landing stays **`●` SSG ×3**, non-dynamic) + lint clean (my files absent from the 50-error pre-existing `set-state-in-effect` baseline). **Full `/verify` driven live against the shared Neon DB** (this slice needs no Blob token): seeded 4 published + 1 draft → `next dev` landing in all three locales showed exactly the 3 newest (`vtest-newest/new/mid`), 4th-published + draft correctly excluded, newest-first, dates/titles/chrome localized (`Latest news`/`Najnowsze aktualności`/`Останні новини`, `9 July 2026`/`9 липня 2026 р.`), `/news` + `/news/[slug]` links per locale; deleted all rows → news section gone from `/`, `/en`, `/ua` (0 matches). All acceptance criteria ticked, issue #22 closed. Only my eight slice files committed (`latest-news.tsx`, `landing-view`, `landing.css`, `news.css`, `news-data`, three catalogs); pre-existing `landing/invite` + settings edits excluded.
- 2026-07-13 — PRD #19 slice (issue #23): on-site news slice 4 — images via Vercel Blob (cover + inline), commit `df1c9fa`. New dep `@vercel/blob` + `BLOB_READ_WRITE_TOKEN`. `uploadNewsImage(formData)` (`news-actions.ts`) is admin-gated via `getAdminSession` and **returns `{ url } | { error }` instead of redirecting** (invoked directly from the client, not a form-POST), rejects non-images and an unconfigured token with a clear message, uploads to Blob with `addRandomSuffix` (no GC in v1 — orphans accepted per contract). `NewsArticleForm` gains a `coverImageUrl` field (upload → preview → replace/remove, hidden input persisted by create/update) and a per-locale **Insert image** button that uploads and splices `![](url)` at the body-textarea cursor (`selectionStart/End`, append fallback); the edit page now hydrates `coverImageUrl` into `initial`. **Publish now requires a cover** on the stored row (mirrors the six-field text check; published edits held to the same bar; drafts may lack one). Covers render on `/news` cards + landing "latest news" cards (link-wrapped 16:9 thumbs, `.news-card__cover` in `landing.css`) and as a hero atop the article page (`.news-hero` in `news.css`); inline photos ride the existing `react-markdown` body. Rendering uses plain `<img>` + the repo's `no-img-element`-disable idiom (Blob is the CDN, no `next/image`/`remotePatterns`). Verified: typecheck + build (all 79 pages prerender, `/news` stays SSG ×3) + `/code-review` (high, no correctness bugs) + lint clean (my files absent from the pre-existing `set-state-in-effect` baseline). **Live Blob round-trip NOT run** (upload cover+inline → publish → images visible; publish-without-cover rejected): needs `BLOB_READ_WRITE_TOKEN` (absent locally) + a Neon branch, and I declined to seed the shared DB behind `.env.local` — those four acceptance boxes stay unticked, **issue #23 left open** for `/verify` on a Vercel preview. `BLOB_READ_WRITE_TOKEN` documented in the local (gitignored, per repo convention) `.env.example` + the action comment + the runtime guard message. Only my eleven slice files committed (`news-actions`, `news-article-form`, admin edit page, `/news` list + `[slug]` pages, `latest-news`, schema doc, `landing.css`, `news.css`, `package*.json`); pre-existing `landing/invite` + settings edits excluded.
- 2026-07-28 — PRD #26 slice 1 (issue #27): migration 0011 lands the feature's schema foundation — `event_heats`, five new `event_registrations` columns (`heat_id`/`confirmed_at`/`bib_returned_at`/`notified_heat_id`/`notified_heat_time`), `participation_status += 'confirmed'`, and the ADR 0003 index swap to `(event_slug, bib) where bib is not null and bib_returned_at is null`, plus a hand-added backfill so rows that kept a bib after leaving `checked_in` don't shrink the pool (no-op on live data). `bibPool` 50 / `heatIntervalMinutes` 10 on individual-event config with `getBibPool`. Bib layer reworked to leases: `suggestNextBib` is the lowest unheld bib in `1..bibPool` (null on exhaustion), check-in leases and clears `bib_returned_at`, `checkInWithoutBib` marks present bib-pending when the pool is empty, reverting releases the lease while retaining `bib` for the record. The widened status union is handled everywhere it renders (roster stats/filters/total, `StatusPill`, `profile.registrations.status.confirmed` in pl/en/ua, `eligibleForEvent`). **Latent bug fixed:** Drizzle 0.45 wraps driver errors in `DrizzleQueryError`, so `isUniqueViolation` never matched — the "bib taken" message and the auto-assign retry loop were both dead code; it now walks the cause chain. Applied to the **live DB** (owner's call, still no neonctl); round-trip verified with a throwaway fixture on `mile-2026-08-29` — 18 checks (lease/release/recycle, pool exhaustion, bib-less check-in, second-holder rejection), live rows untouched, fixtures deleted. Commit `97e5a95` sits on a local `main` 4 commits behind `origin/main` (#24, #13) and still needs integrating.
- 2026-07-28 — PRD #26 slice 2 (issue #29): the admin heat builder at `/admin/events/[slug]/heats` — `generateHeats` / `updateHeat` / `deleteHeat` / `assignToHeat` / `unassignFromHeat` in `heat-actions.ts` over a `heats-data.ts` layer (`getEventHeats` with per-heat fill, `getSeedPool`, `outOfOrderHeats`, `setHeatForRegistrations`). Generation **appends** from the highest existing heat number and never renumbers on delete — a heat number is something runners get emailed, so it is not reshuffled; deletion leans on the `heat_id` `on delete set null` FK to unassign members without touching registrations. Capacity is hard-capped at `bibPool` on both create and update; fill-vs-capacity is shown but **overfill is allowed** (red `n/cap` + "over capacity") on the PRD's warn-don't-block principle — flagged on the issue for the owner to overrule. Out-of-order start times warn per offending heat number. New `heat-time.ts` is the single Warsaw-wall-clock↔instant seam, resolving the offset per instant via `Intl` rather than the hardcoded `CEST` the mailings module uses, so a stored `scheduledAt` can't shift; `HEATS_OFFSET_MINUTES` was extracted in `timetables/index.ts` so the racing-window prefill and the public timetable can't drift apart. Seeding pool = confirmed **plus anyone already in a heat**, so a runner who checks in mid-morning doesn't vanish off the card. **Contract deviation surfaced, not silently shipped:** the PRD's "Client islands: None new" is contradicted by `heat-builder.tsx` — one selection has to span the Unassigned column and every heat column, and the checkboxes must sit outside the bulk-move `<form>` because the per-heat edit/delete forms are siblings (nested forms are illegal), so `recipient-multiselect.tsx` could not be reused verbatim; the `Set<registrationId>` idiom is. Verified: typecheck/build/lint clean (new files silent under eslint), admin gate streams no data unauthenticated in all three locale prefixes, no pl/ua keys added. 50-check data-layer round-trip against the Neon DB the owner supplied (same host as live — fixtures on `mile-2026-08-29`, deletes scoped to created ids, live rows untouched). The authenticated browser walkthrough is the owner's, driven from `scripts/seed-heats-fixture.ts` (untracked, `--teardown` included) — the shell route to an admin session was blocked by the permission classifier.
- 2026-07-29 — PRD #26 slice 3 (issue #30): publishing the heat card and telling the runners. `publishHeats` (`heat-actions.ts`, admin-gated) wraps `publishHeatsAndNotify` (`event-mailings/heat-assignment.ts`) and returns `{notified, skipped, failed}` in the flash. `publishEventHeats` stamps `published_at` on **only the still-null** heats, so re-pressing after an edit does not rewrite the instant runners were first told about; `finished_at` is never touched. The re-notify delta is one function — `heatNotifyState` in `heats-data.ts` (`none` / `stale` / `notified`, comparing `notified_heat_id` and `notified_heat_time` by instant) — read both by the mailing's filter and by the builder's new "To notify" stat, so the count on the button and the set that gets mailed cannot disagree. `no_show` is excluded from the mailing but stays seeded on the card so an admin can take them off; `checked_in` is *included*, because a runner already at the desk can still have their heat moved. Exactly **one** new email kind: additive migration `0014` adds `heat_assignment`, and first-notice vs "your heat has changed" is a render branch inside a single `EventHeatAssignmentEmail`, driven by whether `notified_heat_id` is null — not a second kind. Copy (`heatAssignmentMailContent` + `heatAssignmentSubject`, pl/en/ua) labels the time approximate in the body, the caveat line **and** the subject; the CTA reuses `UI[locale].ticket`. Heat number + approximate start now also render read-only on the profile registration card and the ticket page via `publishedHeatsByRegistration` — **published heats only**, and suppressed once the event is `completed`; two new keys (`profile.registrations.heat` / `heatValue`) in all three catalogs, read by both surfaces so "Heat" exists once per language. **Latent bug fixed:** Resend reports API failures in `error` rather than throwing, so the unchecked `resend.emails.send` idiom would have stamped `notified*` for mail that never left — and re-publishing would then skip that runner forever; the send now throws on `error`. The same unchecked pattern still sits in the media-live and ticket mailings — flagged on the issue, not fixed here. `heat-time.ts` moved `features/admin` → `lib/events` (the runner-facing surfaces read the same Warsaw clock as the admin). Migration `0014` applied to the **live DB** (owner's call); note `0012`/`media_live` is still stranded below drizzle's watermark and `0014` does not repair it. Verified: typecheck/build/lint clean (my files eslint-silent; the 50 remaining errors are the documented `set-state-in-effect` + worktree-sweep baseline) and a 46-check DB round-trip on the empty `mile-2026-08-22` with **no mail transport** (`resend === null`, asserted before the script touches anything) — publish stamping and its idempotency, the delta before/after a simulated delivery, a moved start time, a runner moved between heats, a late walk-up, the seeded no-show, the `heatNotifyState` truth table, and #29's fixture on `mile-2026-08-29` left intact. **The real send path is unverified by me** — success → stamp `notified*` → insert the `event_email_log` row needs an actual delivery, and an earlier draft of that script leaked 3 messages to `@example.invalid` through the live Resend key (static imports hoist above a `process.env` assignment), so the rewritten script refuses to run with a transport configured. Slice #28 (attendance confirmation) was finished in parallel and shares `copy.ts`, the three catalogs, the profile page and the ticket page, so it rides in commit `01624b3` rather than in a commit that would not build; issue #28 is left open and untouched.
- 2026-07-29 — PRD #26 slice 4 (issue #28): attendance confirmation, the remote pre-race "I am coming". `confirmAttendance` (`event-registration/confirm-actions.ts`) is satisfied by the owning session **or** a valid `event-ticket` signature, either alone — guests register passwordlessly (ADR 0002) and may have no password months later, so a sign-in wall in front of a yes/no question would strand them. The return URL is rebuilt server-side from a `surface` token (`profile` | `ticket`) rather than accepted as a caller-supplied path, so the action cannot become an open redirect. `confirmation.ts` holds the data layer and the window: `confirmRegistration` puts the status predicate *inside* the `UPDATE`, making idempotency and concurrency-safety the same mechanism (two simultaneous confirms cannot both write; the second reports `already`), and `confirmedAt` is never overwritten because re-answering must not move the runner’s answer. The window opens at `sendAt("reminder_7d", event)` — reusing the mailing schedule so the ask on the site and the ask in the inbox cannot drift apart — and closes once any heat is published, but it gates **display only**: the action still accepts a late confirm, per the PRD’s "closing the window is not a lockout". Both surfaces call the one action through a shared `ConfirmAttendanceForm`, a plain server-action `<form>` and deliberately not a client island, because the ticket page is reachable signed-out and confirmation must work with no JavaScript. The reminder ask is a conditional callout inside the existing 7d/3d/1d templates driven by the recipient’s status (`EventRecipient` now carries it), never on `morning`; **its button links to the ticket page rather than confirming on click** — a GET that mutates would be fired by corporate mail-gateway link scanners, silently marking whole inboxes as attending. Seven keys under `profile.registrations.confirm` in pl/en/ua, read by both surfaces. Verified: typecheck/build/lint clean (eslint exit 0 on every touched file) plus **84 checks, 0 failures** — 42 on the data layer, window, signature gate and all 12 reminder render permutations, and 42 over HTTP driving the real no-JS path (scraped server-action form, multipart + `Origin`): signature rejection discloses nothing, the CTA renders localized in all three locales, a signed-out guest confirms end-to-end (303 → `c=confirmed`, DB flips), the CTA disappears afterwards, and a bad signature is denied with the DB unchanged. Live-DB fixtures on `mile-2026-08-01`, deleted by id, zero leftovers. **Not driven:** the signed-in profile render and with it the owning-session branch of the guard — it needs an authenticated session and a third session was concurrently rewriting `better-auth.ts`/`sign-in-form.tsx`, so two acceptance boxes stay unticked and **issue #28 is left open**. This slice rides in commit `01624b3` (#30’s), which swept it up from the shared tree; history was left alone because a session was live in it. **Prerequisite `0458bf8`:** merged 28 commits of `origin/main` (news, broadcasts, media gallery, admin users) and regenerated #27’s colliding `0011` as `0013_bitter_lily_hollister` on top of `0012` so the snapshot chain is correct — its SQL hashes to the value already stored in the DB, proving byte-identity. It also repaired a live-DB fault: `0012_solid_wolfsbane` (`media_live`) had never been applied and was **permanently unreachable**, because drizzle only applies journal entries whose `when` exceeds the newest `created_at` and #27’s row sat above it; the DDL was applied, `0012` recorded and #27’s row re-stamped, so `db:migrate` is a clean no-op. The #30 entry above says `media_live` is still stranded — true when written, fixed in `0458bf8`. **Pre-existing finding, unrelated:** `/tickets/[registrationId]` answers **HTTP 200** with the not-found body on a bad or missing signature (a `@modal/[...catchAll]` parallel-routes quirk); no data leaks — verified explicitly — but the status code is wrong for anything consuming it. Worth its own issue.
- 2026-07-29 — PRD #26 slice 5 (issue #31): the public start list at `/[locale]/events/[slug]/heats`, commit `96c71c6`. `getEventStartList` in a new `features/event-heats/start-list.ts` is the narrow public projection of the same rows the admin builder reads — `registrationId`, `name`, `club`, and nothing else; the admin's `heats-data.ts` stays admin-scoped. Rendered one block per heat (number + approximate time as the heading, runners alphabetically underneath) rather than a flat table with a heat column, which is what a start list looks like on a stadium wall. Three flow states: the list, an explicit "not published yet" while the card is still draft, and an empty state for no heats at all; `upcoming` and `completed` short-circuit to empty without a query, per the PRD's "upcoming and completed show nothing". **The caching decision is the load-bearing one.** `generateStaticParams` returns an **empty array** — deliberately present, not omitted: per `node_modules/next/dist/docs/.../generate-static-params.md` ("All paths at runtime"), an empty array renders each path on first visit and then caches it, whereas *omitting* the export re-renders on every request and would make `revalidatePath` a no-op and put two queries on every spectator refresh. Confirmed empirically — after seeding a heat the page kept serving its cached empty state until the entry under `.next/server/app/<locale>/events/<slug>/heats.*` was cleared, which is exactly what `revalidatePath` invalidates. `revalidateBuilder` became `revalidateHeatSurfaces` and now also calls `revalidateStartList()` (route-pattern form, one call covering pl/en/ua and every event, the `revalidateNews` idiom); that covers generate too, because the first draft heat flips the public page from empty to "not published yet". Check-in deliberately does **not** invalidate it. **`markHeatFinished`/`unmarkHeatFinished` (#32) must call `revalidateStartList()` — they do not exist yet, and the obligation is recorded at the helper rather than left silent.** The read model carries **no status filter**, reversed during review: filtering out a `no_show` would make the page depend on a column only check-in writes, on a page check-in must not invalidate — a filter that never actually runs. The projection now depends on exactly what invalidates it (which heats are published, who is in them); a runner who should not appear is unassigned, which is a heat edit. New `events.heats` namespace, 15 keys × pl/en/ua, reusing the mailing's established terminology (`Seria`/`Забіг`, `Lista startowa`/`Стартовий список`) so the page and the email a runner holds say the same word. **Beyond the AC:** a start-list link on the event detail page for `registration_closed` events — the page was otherwise unreachable from the site (user stories #10–12), config-derived so the detail page stays SSG; flagged on the issue. Also retired the third copy of the same `Intl` date block into `formatEventLongDate` (`lib/events/time.ts`), now shared by the detail, gallery and start-list pages. Verified: typecheck/build/lint clean, and driven over HTTP in all three locales through **all three states** with live-DB fixtures on `mile-2026-08-01` (owner's explicit sign-off in-session) — `publishHeats` was **never called**, `published_at` was stamped directly, so no mail left the building; times render 10:20 Warsaw from an 08:20Z instant, no bib appears in the markup, and cleanup asserted zero leftovers with #29/#32's fixtures on `mile-2026-08-29` untouched. Because the shared checkout was mid-edit by the race-morning session and did not typecheck, the whole gate ran in a throwaway worktree at HEAD carrying only this slice. **Pre-existing, confirmed not mine:** `notFound()` answers HTTP 200 with the 404 body here exactly as it already does on `/events/[slug]/gallery` and `/news/[slug]` — the same quirk the #28 entry logs for the ticket page.
- 2026-07-29 — PRD #26 slice 6 (issue #33): the race-morning heat export, commit `cab3e3a`. A **new** event-series exporter, `features/admin/heat-export.ts`, behind `GET /api/admin/events/[slug]/heats/export` (admin-gated via `getAdminSession`, `runtime = "nodejs"`, byte-for-byte the shape of the existing roster-export route). One workbook, three sheet kinds: a **Flat** sheet (every registration — heat, start, bib, first/last name, sex, club, status, checked-in), added first so it opens by default for the RaceResult timing operator; **one sheet per heat** (`Heat N`) for the start line, empty heats included; and an **Unassigned** sheet of confirmed/checked-in runners in no heat, so a walk-up or an unseeded confirmation is never silently absent. Every sheet leads with an italic banner row stamped `Generated <date time> Warsaw` (plus `workbook.created`) — the stale-printout guard has to survive being printed, so it lives in a cell, not just file metadata; heat start times live in the per-heat banner because a colon is an illegal worksheet-name character. Bibs read straight from the live lease (ADR 0003): blank before check-in, the leased number after. **Deliberately self-contained on the committed schema** — its own `registrations ⋈ users ⋈ heats` query mirroring `start-list.ts`, *not* a reuse of the roster read model, because `RosterRow`'s heat columns were still uncommitted in the shared tree (a sibling slice) and coupling to them would have wedged this commit behind that one; the only cross-slice dependency is `getEventHeats` (#29, committed). The frozen `export-runners.ts` is untouched. An "Export Excel" link joins the heat-builder action bar (admin English-only, reusing `DownloadLink` — no new i18n keys, matching the roster page). Verified: `tsc` clean on **HEAD + this slice only** (the live checkout was mid-churn from concurrent #32/roster work in files I don't own, so the gate ran in a throwaway worktree at HEAD; `next build`'s SSG pass doesn't apply — the route is a dynamic `nodejs` handler and the edited page was already dynamic — and Turbopack rejects a junctioned `node_modules`, so build was validated by `tsc` + the route mirroring a route that already builds); `eslint` silent on every touched file. Runtime, live DB read-only on `mile-2026-08-29` (9 heats, 8 regs): 11 sheets in order (Flat, `Heat 1–9`, Unassigned), stamp present, headers correct, one flat row per registration, bibs blank (0 filled / 0 bibbed — no live check-in exists yet anywhere). The leased-bib half of the AC was proven with a **scoped self-owned fixture** (a `confirmed` registration with `bib 47` on `mile-2026-08-29`) that rendered `47` in both the Flat and Unassigned sheets, then deleted by id with 0 rows left behind — no existing row was ever mutated. `scripts/verify-heat-export.ts` left untracked, per the fixture-script convention. Issue #33 closed, all six ACs ticked.
- 2026-07-29 — PRD #26 slice 7 (issue #32): the race-morning on-site flow, commit `152dde7`. The participant's signed ticket page grows an **inline admin panel** (`features/admin/components/ticket-admin-panel.tsx`) when `getAdminSession()` returns a session: runner details, their heat, and one Check in button that leases the lowest free bib and shows it large. The QR target is unchanged because issued codes are baked into ticket emails already sent; a non-admin — the ticket's owner included — sees exactly the page they saw before, and **rendering the panel is not authorization**: driven over HTTP, a panel-shaped POST with no admin cookie redirects to `/admin/login` and changes nothing. Rather than a second check-in action (which the frozen Contracts table does not have), `assignBibAndCheckIn` gained a `surface` token and rebuilds its return URL server-side — the idiom `confirmAttendance` already uses — so the desk and the scanned ticket run one action and report through one set of flash codes, worded once in `checkin-copy.ts`. `markHeatFinished` stamps `finishedAt` and returns every held bib in one transaction; `unmarkHeatFinished` refuses outright and names the numbers if any has been re-leased, writing nothing when it refuses (ADR 0003). A **draft** heat cannot be finished — nobody was told to run it, so `heatState` can no longer skip from unreleased to run. Walk-ups auto-seed into the earliest published heat with a free lane, guarded on `heat_id is null` so a deliberately seeded runner is never moved; where none has room they stay checked in and chipped and surface on an **Unplaced** list, alongside a **bib-pending** waiting list in arrival order with the button to hand a freed number over. The sharp lesson: *holding no bib is not the same as needing one* — after a heat finishes its runners are still `checked_in` with numbers returned, and the first cut offered them all a fresh bib, which would have handed the twelve numbers a heat just freed straight back to the twelve people who had finished racing. `awaitingBib` is now the predicate both lists and the desk read, and `leaseBibForCheckedIn` refuses them at the data layer too. Bib **search** now matches the current holder only: under recycling a number is worn by several runners across a morning, so the desk's QR fallback had been returning a growing pile of ex-holders. Walk-up placement and finish/unfinish call `revalidateStartList` (#31) — the public list projects heat membership, so those three change it even though a plain check-in cannot, which is exactly the seam that module's own comment reserved for this slice. Verified: 62-check DB round-trip on `mile-2026-08-15` fixtures (deletes scoped to created ids, live rows untouched) plus the full path over HTTP on a real dev server — signed-out ticket in pl/en/ua with no panel markup, admin check-in leasing bib 1 and auto-seeding heat 1, finish returning it, un-finish re-leasing it, and the desk gated to the login page unauthenticated in all three locales.
- 2026-08-06 — PRD #34 slice 1 (issue #35): the admin application shell, commit `f098489`. The chrome moves out of the per-page `AdminShell` wrapper into an admin-level `layout.tsx`, so every page under `/admin` inherits it instead of opting in; `AdminShell` is deleted and all 13 pages now render `AdminPage` (topbar + `<main>` column, 1400px / 560px `narrow`). New `src/app/admin.css` declares the dark `--admin-*` token set on `.admin-root`, exposed through `tailwind.config.ts` as the `admin-*` colour scale plus two admin radii — the first Tailwind admin layer under ADR 0004, with `.ace-landing .iv` deliberately left on the same element so Users, Admins, Inquiries, News, Legacy and Mailings keep rendering through their frozen `.iv-*` rules inside the new shell. `shell/admin-sidebar.tsx` is one element with two behaviours (fixed rail from `lg`, off-canvas drawer below it with scrim + Escape + close-on-navigate); its nav model is built server-side in `admin-nav.ts` from `getIndividualEvents()` and handed over as plain data, so the registry — and the results payloads it imports — stay out of the client bundle. Two decisions worth carrying forward. **The topbar cannot live in the layout**: a layout cannot read its children's title and page actions, and the AC puts both there, so the layout owns sidebar/root/drawer and each page contributes its topbar — at the cost of one extra `getAdminUser()` per page alongside the page's own `requireAdmin`. **A layout above the guard streams before it**: with the shell one level up, a guest's first response now carries the sidebar chrome (nav labels + event dates, no admin data) before the redirect fires; this follows from the PRD's "the layout is presentation only, never the sole guard" and is *not* caused by the new `loading.tsx` — removing it left the 200 + in-band `;307;` unchanged, and the untouched `/profile` behaves the same because `[locale]/loading.tsx` already streams above it. `/admin/events` ships as a registry-only stub so the sidebar's "All events" link isn't dead before #38. Verified: static gate clean, plus the full shell over HTTP on a `next build` + `next start` server (13 routes × sidebar/topbar/title/content, one `aria-current` per page with the event tabs highlighting their event, identical English render under all three locale prefixes, guest gate by content marker) and screenshots at 1440×900 and 390×844. `next dev` is currently unusable in this environment — Turbopack's PostCSS worker dies with `0xc0000142` on the untouched `globals.css`; `next build`/`next start` are unaffected. Left unticked: non-admin → not-found (no non-admin session driven) and the no-database notice (not runtime-reachable — the branch renders only after `requireAdmin`, which itself needs the database), so #35 stays open.
- 2026-08-06 — PRD #34 slice 5 (issue #39): the shared per-event layout, commit `95ac94b`. One `layout.tsx` at `/admin/events/[slug]` states the event once — long date as the heading (the series shares one name, so the date is what distinguishes a night), venue + racing window, `EventStatusBadge` from the registry, five stat tiles, and the event-level actions — then a Roster/Heats/Check-in tab bar, then the tab. The three pages drop their `AdminPage` wrapper, their cross-link clusters, both `DownloadLink` exports and the media-live form, and are bare fragments again. Three things worth carrying forward. **This layout guards, unlike the shell layout above it.** #35's entry records that a layout streams before the guard below it; that was harmless there because the shell renders nothing admin-only, but this one renders registration counts, so it calls `requireAdmin` itself — the `loading.tsx` beside it is a Suspense boundary *between* the layout and its pages, so the header would otherwise flush before a page's guard ran. It is still not the sole guard: layouts are skipped on client-side navigation between their own pages, so every tab keeps its own. **Runtime data in a layout needs its own Suspense boundary** (`layout.md`/`loading.md`): the counts stream inside one and degrade to "—" on a database error rather than taking the race-morning check-in desk down with them; `getRosterStats` and `getUser` are now `cache()`-wrapped, which also collapses #35's "one extra `getAdminUser()` per page" into a single session read. **Two content markers that look like they work but don't:** `"This page could not be found"` is inlined into every page's payload here so a not-found assertion on it always passes, and `res.url` never moves on a streamed `redirect()` — use the robots-noindex tag and the `<meta http-equiv="refresh">` tag instead, with a control route to prove they can fail. Verified: static gate clean, and 152/152 HTTP checks on a `next build` + `next start` server against the live database (header + active-tab state on all three tabs under all three locale prefixes, guest sent to sign-in with `redirectTo` intact and nothing leaked, unknown slug + frozen team event not-found, both xlsx endpoints serving workbooks, real counts in the header). Only gap: the photos-live button's positive branch has no fixture — no registry event has `media` configured yet. The shared working tree had interleaved this slice with #36/#37/#38 inside the same files, so the commit also carries #36's flash module and three leaf components from #37/#38 purely to keep `HEAD` building; those issues stay open.
- 2026-08-06 — PRD #34 slice 3 (issue #37): the next-event-first dashboard, commit `061c944`. The page is now a composition — `FeaturedEventCard` → `SeriesStrip` → secondary `AdminStat` tiles → a one-line news teaser — and the old `Stat`/`.iv-table` body is gone. The hero heads itself with the **long date, not the name**, for the same reason #39's event header does: every night is called "Individual Mile", so the date is the only thing that distinguishes one; it carries the registration total in accent plus its awaiting/confirmed/checked-in breakdown, and the three surface links. The strip lists all five individual events with completed nights dimmed but still linked, the featured one ringed; `getIndividualEvents` keeps the frozen team event out, asserted over HTTP rather than assumed. **The no-database branch changed shape rather than being preserved verbatim**: instead of the notice *replacing* the body, the page passes `null` stats into the same sections, so the registry half still renders with counts reading "—" — one component tree, two data states, which is also what made the strip's zero-count rendering free. **One contract judgement call:** the AC asks for a "recent registrations" tile, which no existing read answers; rather than a new data module this is a fourth count inside `getOverviewStats`' existing `Promise.all` (`recentRegistrations`, window constant `RECENT_REGISTRATION_DAYS = 7`), keeping the PRD's "all data access goes through existing read paths". The hero's breakdown reuses the roster header's own `getRosterStats`; the strip's per-event totals come from the single grouped overview query, so the page adds no per-event fan-out. Verified: static gate clean on the merged tree (lint byte-identical to baseline, nothing under `admin/`), 82/82 HTTP checks on a `next build` + `next start` server against the live database (hero + strip + tiles under all three locale prefixes, cross-checked numbers — hero `7` = 2 + 5 + 0 matching the strip's own `7 registered` — guest sent to sign-in with `redirectTo` intact and **zero** dashboard content in the payload), and a fixture round-trip proving the new window predicate counts an entry taken now and ignores one backdated past seven days, with the counter back to baseline afterwards. Left unticked: the no-featured-event branch (registry state — forcing it means editing `registry.ts` or adding a throwaway route in a checkout two other sessions are building from) and the no-database notice (not runtime-reachable, same as #35), so #37 stays open. The `next dev` breakage #35 logged is still live — `0xc0000142` in Turbopack's PostCSS worker — so the whole HTTP gate ran on `next start`. Shared-tree note: #38's session was writing the same shell primitives concurrently; `admin-stat.tsx`/`admin-button.ts` were reconciled into one superset each and swept into #39's commit, and this commit carries their `admin-empty-state.tsx` because the hero's empty state imports it.
- 2026-08-06 — PRD #34 slice 2 (issue #36): the centralized admin flash, commits `95ac94b` (the module, carried in by #39 — see below) and `637106f`. `features/admin/flash.ts` is one code→sentence registry for the whole panel: `resolveFlash(searchParams, { slug })` returns `{ tone, message }` or `null`, error outranking ok, with `?msg=` — the older channel where the action writes the sentence itself — kept as neutral info because the actions and their redirect contracts are frozen. Entries are functions of the query plus a small context, so `capacity` can name the event's real bib pool from the registry and `published` can compose its four counts, which is what let the heats page's local `okText`/`errorText` maps be deleted outright rather than moved. Rendering splits in two: `components/admin-flash.tsx` is a **server** component that resolves the copy (the registry reaches `heats-data`, so it must never enter a client bundle) and `components/flash-banner.tsx` is the client island holding only dismissed + auto-hide, on the ADR 0004 admin tokens — no `.iv-*` added, no i18n keys, `role="alert"` for a refusal and `role="status"` for a confirmation, ok hiding at 6s and errors at 12s with the timer held while the pointer or focus is on the banner. `plural` moved from the desk-only `checkin-copy.ts` up to `format.ts`, where the registry, the desk and `race-morning` all read it. **The non-obvious part is the `key`.** Client state survives a server-action round-trip, and pressing the same button twice redirects to a URL *identical* to the current one, so a code-derived key would leave the second press showing a banner the admin had already dismissed — i.e. no feedback at all. The server component therefore mints a fresh `crypto.randomUUID()` key per request; proven over HTTP by two identical requests carrying different keys in the flight payload. The cost, written into the file, is that any re-render un-dismisses — acceptable because nothing under `/admin` re-renders without a fresh action (no `router.refresh()` exists there). **The check-in desk is deliberately not centralized** and keeps `checkin-copy.ts`: its `?ok=` is not a code but the bib just leased (`ok=12`), which a code→sentence map cannot express, and the same module words the admin panel on the *public* ticket page, which gets no admin banner. `flash.ts` names that gap and the two collisions #43 must resolve when it folds in (`input`, and `heat_missing` vs `missing`). Verified: typecheck/build/lint clean, then 22/22 content-marker checks over HTTP against a `next build` + `next start` server — every registry code, singular/plural, the composed publish counts, error-beats-ok, the roster's `?msg=`, four negative controls (plain page, unknown ok, unknown error, empty value) proving unknown codes render *nothing*, and identical English under `/admin`, `/en/admin`, `/ua/admin` — plus a **real action round-trip**: a multipart no-JS post of the generate form with an empty count returned `303 → ?error=count` and the redirect target rendered the registry's sentence. That path writes nothing, so the only live-DB write was a throwaway admin session, torn down by id. Shared-tree note: #39's session was rewriting all three event pages at the same time and committed this slice's module inside `95ac94b`, leaving `race-morning.tsx` importing a `plural` that no longer existed — HEAD did not typecheck until `637106f`; the gate for this slice ran in a throwaway worktree at HEAD (Turbopack rejects a junctioned `node_modules`, so it needed its own `npm ci`). Dismissal itself is the one AC not driven: there is no browser automation in this environment.
- 2026-08-06 — PRD #34 slice 4 (issue #38): the admin events index, commit `9e04cbf`. `/admin/events` stops being #35's registry-only stub and becomes the Events group's real landing page: a 1/2/3-column card grid, one card per individual mile event, each headed by its **long date rather than its name** (the third surface to land on that reasoning after #37's hero and #39's event header — every night is called "Individual Mile"), with venue + racing window, an `EventStatusBadge` off the registry, `Registrations` and `Checked in` counters, and Roster/Heats/Check-in links. Counts are `getRosterStats(slug)` fanned out one grouped query per event inside a single `Promise.all` — the same read the roster header uses, so the page adds no data path, action, schema or message key; `Registrations` is the sum of the four live statuses, matching how the roster header computes its own total. **The no-database branch took #37's shape, not #35's**: `stats` is `null` rather than the notice replacing the body, so the cards still render from the registry with both counters reading "—" — the third slice to converge on "one component tree, two data states". Completed nights recede at 60% like the sidebar and #37's strip, but with `hover:`/`focus-within:opacity-100` on top, because unlike those two single-link surfaces a dimmed card here would mean three dimmed controls. Verified: static gate clean, then over HTTP on a `next build` + `next start` server against the live database — exactly 5 cards inside `<main>` with statuses matching the registry, rendered counts (29/7/0/0/8) cross-checked against `getRosterStats` read straight out of the database, all 15 quick-link hrefs, zero `warsaw-2026` occurrences proving the frozen team event is absent, one `aria-current` on "All events", identical English under all three locale prefixes, and a guest gate asserted by content marker against a control route. **The counter assertion needed a fixture to mean anything**: every event has `checked_in = 0` today, so `Checked in` rendering 0 was equally consistent with it reading `no_show`; a scoped `checked_in` + `no_show` pair on `mile-2026-08-15` made the card read `Registrations 2 / Checked in 1`, then came out by created id with the card back to 0/0. Left unticked: `registration_closed` (no event holds that status) and the empty state (needs an empty registry) — both reachable only by editing `registry.ts`, which three concurrent sessions were verifying registry-driven pages against, plus non-admin → not-found and the no-database notice, unchanged from #35. Shared-tree note: this slice and #37 wrote the same shell primitives at the same time; the reconciliation kept #37's supersets (`adminButton()`, `AdminStat`, `EventStatusBadge`) and discarded this slice's parallel versions, and `admin-empty-state.tsx` — written here — ships inside #37's `061c944`, so `9e04cbf` carries only the page plus a dedupe of the lifecycle dot map out of `admin-sidebar.tsx`.
