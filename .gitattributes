# The legal corpus is hash-guarded at build time (scripts/check-legal-manifest.ts):
# its bytes must be identical on every checkout, so git must never rewrite line endings.
src/content/legal/** -text
